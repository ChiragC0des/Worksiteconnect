[build]
  command = "cd client && npm install && npm run build"
  publish = "client/build"

[redirects]
  [[redirects]]
    from = "/api/*"
    to = "/.netlify/functions/:splat"
    status = 200
    force = true
